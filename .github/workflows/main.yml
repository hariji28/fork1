name: Azure Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Validate syntax
      run: |
        # Add your validation command
        az  bicep build --file vm(1).bicep

    - name: Deploy to Azure
      run: |
        # Add your deployment command 
         az login
         az deployment group create --resource-group bicep3 --template-file vm(1).bicep
